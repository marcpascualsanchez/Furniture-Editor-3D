class User{
    constructor(name, email, password){
        this.name = name;
        this.email = email;
        this.password = this.encodePassword(password);
    }

    encodePassword(password){
        let result;

        return result;
    }
}